# Generated by Cargo
/target

# ts-rs derives one #[test] per exported type, and those write here — the
# default location, since they do not use gen-types' Config. Harmless, and
# deliberately not be/src/generated/, so `cargo test` cannot clobber the file
# that actually ships. Ignored because it is a build artifact of the test run.
/bindings
